import { RemoveSlashPipe } from './remove-slash.pipe';

describe('RemoveSlashPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSlashPipe();
    expect(pipe).toBeTruthy();
  });
});
